const threeSum = require('./3sum.js');


describe('should 3 sum', () => {
  test('it should sum 3', () => {
    expect(threeSum()).toBe();
    expect(threeSum()).toBe();
    expect(threeSum()).toBe();
  })
})

